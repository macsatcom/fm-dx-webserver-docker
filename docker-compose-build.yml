services:
  fm-dx-webserver:
    image: fm-dx-webserver:latest
    build:
      context: .
      args:
        VERSION: latest
    ports:
      - "8080:8080"
    devices:
      - "/dev/ttyUSB0:/dev/ttyUSB0"
    restart: unless-stopped
